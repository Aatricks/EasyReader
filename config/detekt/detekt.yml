# Project overrides on top of detekt's default config (buildUponDefaultConfig = true).
naming:
  FunctionNaming:
    # Compose convention: composable functions are PascalCase (per detekt's own Compose guidance).
    ignoreAnnotated: ['Composable']
